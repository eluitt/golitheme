# ---- wp-content (submodule) ----

# 1) دایرکتوری‌های Runtime/Backup/Log (تو ریپوی گیت نروند)
uploads/
upgrade/
cache/
updraft/
wflog/
wflogs/
debug.log
languages/          # فقط languages در ریشه‌ی wp-content؛ زبان‌های تم ربطی ندارند

# 2) پلاگین‌ها: همه Ignore؛ فقط اگر خواستی پلاگین‌های سفارشی‌ات را Whitelist کن
plugins/ل
# نمونه‌ی وایت‌لیست (در صورت نیاز، علامت # را بردار):
# !plugins/golinaab-*/
# !plugins/golinaab-*/**

# 3) تم‌ها: فقط golitheme را Track کن، بقیه Ignore
themes/**
!themes/golitheme/**
# آرتیفکت‌های بیلد تم (فایل بیلد CSS را Track نگه می‌داریم)
themes/golitheme/node_modules/
themes/golitheme/assets/styles/*.map

# 4) فایل‌های ریشه‌ی wp-content
/index.php
/woocommerce-strip.php

# 5) فایل‌های متفرقه و محیطی
*.log
*.tmp
.DS_Store
Thumbs.db
node_modules/
.env
.env.*
